# Copy to .env.local (dev) or .env (compose) and fill in.

# https://console.x.com — app must be in a Project with API access
X_BEARER_TOKEN=
X_CONSUMER_KEY=
X_SECRET_KEY=

# https://jina.ai/ — full-article markdown via r.jina.ai
JINA_API_KEY=
